{
    var conversion=require('./mertric');
    x=10;
    console.log('convert given number to centimeter',conversion.cm(x));
    console.log('convert given number to kilometer',conversion.km(x));
    console.log('convert given number to millimeter',conversion.mm(x));
}